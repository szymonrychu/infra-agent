apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "infra-agent.fullname" . }}-clusterrolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "infra-agent.fullname" . }}-clusterrole
subjects:
  - kind: ServiceAccount
    name: {{ include "infra-agent.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
